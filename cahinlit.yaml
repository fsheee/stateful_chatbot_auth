
chainlit: 2.4.1

# Interface settings
ui:
  name: "Chainlit Chatbot"
  description: "A simple Question Answering Stateful chatbot with GitHub authentication built with Python, UV, and Chainlit."

# Message settings
default_expand_messages: true

# Auth settings
auth:
  required: true
  providers: 
    - github

# OAuth Configuration
oauth_providers:
  github:
    client_id: ${OAUTH_GITHUB_CLIENT_ID}
    client_secret: ${OAUTH_GITHUB_CLIENT_SECRET} 


# @cl.on_message
# async def handle_message(message:cl,Message):
#    history=cl.user_session.get("history")
#    history.append({"role":"user","content":message.content})

#    formated_history[]

#    for msg in history:
#        role="user" if msg["role"]=="user" else "model"|
#        formated_history.append({"role":role,"parts":[{"text":msg["content"]}]})

#        response=model.generate_content(formated_history)

#        response_text=response.text if hasattr(response,"text") else ""
#        history.append({"role":"assistan","content":response_text})
#        cl.user_session.set("history",history)

#        await cl.Message(
#            content=response_text).send()          